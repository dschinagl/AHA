defaults:
  - base_config
  - _self_

dataset:
  root: TODO  # Set to your local ImageNet directory.

explainer:
  dinov3_path: dinov3
  backbone_weights: TODO  # Set to your downloaded DINOv3 ViT-L/16 weights (.pth).

hydra:
  job:
    chdir: false
  output_subdir: null
